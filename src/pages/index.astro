---
import BaseLayout from '../layouts/BaseLayout.astro'
import ChooseNextStep from '../components/ChooseNextStep.astro'
import HomeStoryMobile from '../components/HomeStoryMobile.astro'

const storyBeats = [
  {
    number: '01',
    label: 'Philosophy',
    kicker: 'Welcome',
    title: 'Technique Above Everything',
    body: '',
    image: '/images/home-storyboard/01-kick-clean.webp',
    mobileImage: '/images/home-storyboard/mobile/01-kick-clean-mobile.webp',
    alt: 'Lee Jones striking a football on grass in warm light',
    objectPosition: '50% 50%',
    objectPositionTablet: '54% 50%',
    objectPositionMobile: '61% 50%',
    copyPos: 'center',
    markerX: 24.2,
    markerY: 48,
    maskX: 24.2,
    maskY: 48,
    maskSize: 0,
    ballSize: 'clamp(6.1rem, 7.8vw, 7.2rem)',
    tone: 'dark',
    overlay: 'none',
  },
  {
    number: '02',
    label: 'Origin',
    kicker: 'Origin',
    title: 'It Started With A Bag Of Balls',
    body: 'In 2013 it started with an idea. Joner Football was born. Now we are recognised all over the world.',
    image: '/images/home-storyboard/02-origin-clean.webp',
    mobileImage: '/images/home-storyboard/mobile/02-origin-clean-mobile.webp',
    alt: 'Lee Jones training on the grass with a bag of footballs beside him',
    objectPosition: '56% 50%',
    objectPositionTablet: '62% 50%',
    objectPositionMobile: '70% 50%',
    copyPos: 'top-left',
    markerX: 63.2,
    markerY: 81,
    maskX: 63.2,
    maskY: 81,
    maskSize: 0,
    ballSize: 'clamp(6.8rem, 8.4vw, 7.9rem)',
    tone: 'dark',
    overlay: 'none',
    stamp: 'EST 2013',
  },
  {
    number: '03',
    label: 'Growth',
    kicker: 'Growth',
    title: 'Local Sessions. Global Reach.',
    body: 'The work spread through videos, players, parents, coaches, and millions of touches online.',
    image: '/images/home-storyboard/03-method-filming.webp',
    mobileImage: '/images/home-storyboard/mobile/03-method-filming-mobile.webp',
    alt: 'Lee Jones coaching a technical ball mastery drill indoors',
    objectPosition: '48% 48%',
    objectPositionTablet: '54% 48%',
    objectPositionMobile: '58% 48%',
    copyPos: 'left',
    markerX: 37.2,
    markerY: 72,
    maskX: 37.2,
    maskY: 72,
    maskSize: 0,
    ballSize: 'clamp(5.8rem, 6.8vw, 6.6rem)',
    tone: 'dark',
    overlay: 'growth',
    ballVisible: false,
    ctaPlacement: 'overlay',
    ctas: [
      { label: 'YouTube', href: 'https://www.youtube.com/@jonerfootball', external: true },
      { label: 'Instagram', href: 'https://www.instagram.com/jonerfootball/', external: true },
      { label: 'Facebook', href: 'https://www.facebook.com/Jonerfootball/', external: true },
      { label: 'TikTok', href: 'https://www.tiktok.com/@jonerfootball', external: true },
      { label: 'JF App', href: 'https://app.jonerfootball.com', external: true },
      { label: 'X / Twitter', href: 'https://twitter.com/jonerfootball', external: true },
    ],
  },
  {
    number: '04',
    label: 'Joner Way',
    kicker: 'The Joner Way',
    title: 'A Method Like No Other',
    body: 'Every technical detail gets coached through energy, passion & positivity.',
    image: '/images/home-storyboard/04-joner-way-dsc08725.webp',
    mobileImage: '/images/home-storyboard/mobile/04-proof-growth-mobile.webp',
    alt: 'Lee Jones coaching players during an evening Joner Football session',
    objectPosition: '50% 44%',
    objectPositionTablet: '54% 44%',
    objectPositionMobile: '58% 44%',
    copyPos: 'mid-left',
    markerX: 34,
    markerY: 72,
    maskSize: 0,
    ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)',
    tone: 'dark',
    overlay: 'none',
    ballVisible: false,
    ctas: [
      { label: 'Train With Us In Sydney', href: '/training#jfp' },
    ],
  },
  {
    number: '05',
    label: 'Global',
    kicker: 'Going Global',
    title: 'Coaching Worldwide',
    body: 'From Sydney to coaching in Football Communties around the world.',
    image: '/images/home-storyboard/05-global-camps.webp',
    mobileImage: '/images/home-storyboard/mobile/05-global-camps-mobile.webp',
    alt: 'Joner Football players training at a large outdoor camp',
    objectPosition: '50% 46%',
    objectPositionTablet: '48% 48%',
    objectPositionMobile: '46% 50%',
    copyPos: 'global-bottom',
    markerX: 24,
    markerY: 75,
    maskSize: 0,
    ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)',
    tone: 'dark',
    overlay: 'none',
    ballVisible: false,
    ctas: [
      { label: 'Elite Camps', href: '/camps' },
    ],
  },
  {
    number: '06',
    label: 'JF App',
    kicker: 'The Joner Football App',
    title: 'Worlds #1 Football Coaching APP',
    body: 'Built for players, parents, and coaches to train with clarity, confidence, and purpose.',
    image: '/images/home-storyboard/06-app-session-planning.webp',
    mobileImage: '/images/home-storyboard/mobile/06-app-session-planning-mobile.webp',
    video: '/images/home-storyboard/06-app-header-video-desktop.mp4',
    alt: 'Joner Football App showing session planning content',
    objectPosition: '50% 50%',
    objectPositionTablet: '50% 50%',
    objectPositionMobile: '50% 50%',
    copyPos: 'center',
    markerX: 77,
    markerY: 72,
    maskSize: 0,
    ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)',
    tone: 'light',
    overlay: 'none',
    ballVisible: false,
    ctas: [
      { label: '7 Day Free Trial', href: 'https://app.jonerfootball.com/categories/category-vpi8uazway4', external: true },
      { label: 'App Store', href: 'https://apps.apple.com/au/app/joner-football-app/id6737069275', external: true, badge: 'apple' },
      { label: 'Google Play', href: 'https://play.google.com/store/apps/details?id=tv.uscreen.jonerfootball', external: true, badge: 'google' },
    ],
  },
  {
    number: '07',
    label: 'Our HQ',
    kicker: 'Our HQ',
    title: 'A Home Of Our Own',
    body: 'Sydney, Australia. Belrose. Our home.',
    image: '/images/home-storyboard/07-facility-hq.webp',
    mobileImage: '/images/home-storyboard/mobile/07-facility-hq-mobile.webp',
    alt: 'Lee Jones standing outside the Joner Football HQ facility in Belrose',
    objectPosition: '56% 50%',
    objectPositionTablet: '64% 50%',
    objectPositionMobile: '76% 50%',
    copyPos: 'hq-left',
    markerX: 35,
    markerY: 70,
    maskSize: 0,
    ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)',
    tone: 'dark',
    overlay: 'none',
    ballVisible: false,
    ctas: [
      { label: 'Visit HQ Map', href: 'https://maps.google.com/?q=20+Narabang+Way+Belrose', external: true },
    ],
  },
  {
    number: '08',
    label: 'Action',
    kicker: 'Action',
    title: 'Choose Your Next Step',
    body: 'Train online, train with us, join a camp, or get inside the Joner Football Hub.',
    image: '/images/home-storyboard/08-action-coach-night.webp',
    alt: 'Lee Jones giving coaching detail during a Joner Football session at night',
    objectPosition: '50% 38%',
    objectPositionTablet: '50% 38%',
    objectPositionMobile: '52% 32%',
    copyPos: 'hidden',
    markerX: 18,
    markerY: 77,
    maskSize: 0,
    ballVisible: false,
    tone: 'dark',
    overlay: 'chooseNextStep',
  },
]

const stats = [
  { value: '2M+', label: 'followers online', target: 2000000, format: 'millions' },
  { value: '50K+', label: 'app downloads', target: 50000, format: 'thousands' },
  { value: '1,500+', label: 'training drills', target: 1500, format: 'comma' },
  { value: '15+', label: 'countries reached', target: 15, format: 'plain' },
]


const primaryPaths = [
  { title: 'JF App', text: '1,500+ drills, programs, session plans, and coach detail.', href: '/app', tag: 'Train online' },
  { title: 'Sydney Coaching', text: 'JFP, juniors, pro sessions, and year-round technical training.', href: '/training', tag: 'Train in person' },
  { title: 'Elite Camps', text: 'High energy Joner camps across Australia, the USA, and worldwide.', href: '/camps', tag: 'Camps' },
  { title: 'The Hub', text: 'Free articles, challenges, resources, and clear training pathways.', href: '/hub', tag: 'Learn' },
  { title: 'Coaches', text: 'Coaches section, workshops, session plans, and methodology.', href: '/app/for-coaches', tag: 'Coach better' },
  { title: 'Shop', text: 'Official Joner apparel, training gear, and football resources.', href: '/shop', tag: 'Gear' },
  { title: 'Our HQ', text: 'Visit the Belrose indoor facility and train at Joner Football HQ.', href: '/hq', tag: 'Sydney home' },
  { title: 'Blog', text: 'Drills, player development, coaching tips, and camp stories.', href: '/blog', tag: 'Read' },
  { title: 'Teams', text: 'Bring Joner Football App access and training plans to your squad.', href: '/teams', tag: 'Clubs' },
]

const actionLinks = [
  { label: 'The App', href: '/app', className: 'app', eyebrow: 'Train online', icon: 'APP' },
  { label: 'Train in Person', href: '/training', className: 'train', eyebrow: 'Sydney sessions', icon: 'JFP' },
  { label: 'Coaches Section', href: '/app/for-coaches', className: 'coaches', eyebrow: 'Plan better', icon: 'CO' },
  { label: 'Elite Camps', href: '/camps', className: 'camps', eyebrow: 'Worldwide', icon: 'CA' },
  { label: 'Workshops', href: '/workshops', className: 'workshops', eyebrow: 'Learn live', icon: 'WS' },
  { label: 'The JF Hub', href: '/hub', className: 'hub', eyebrow: 'Free resources', icon: 'HUB' },
  { label: 'Player', href: '/app', className: 'player', eyebrow: 'Improve your game', icon: 'PL' },
  { label: 'Parent', href: '/training#joners-juniors', className: 'parent', eyebrow: 'Find a pathway', icon: 'PA' },
  { label: 'Coach', href: '/app/for-coaches', className: 'coach', eyebrow: 'Coach with clarity', icon: 'CH' },
]

const journeyLinks = [
  { label: 'Joners Juniors', href: '/training#joners-juniors', className: 'juniors' },
  { label: 'Joner 6-8 Year Old Program', href: '/about#contact', className: 'sixEight' },
  { label: 'Joner Football Performance', href: '/about#contact', className: 'performance' },
  { label: 'Start Your Journey', href: '/about#contact', className: 'start' },
]

const mediaStyle = (beat) => {
  const desktop = beat.objectPosition || '50% 50%'
  const tablet = beat.objectPositionTablet || desktop
  const mobile = beat.objectPositionMobile || tablet
  return `--story-object-desktop: ${desktop}; --story-object-tablet: ${tablet}; --story-object-mobile: ${mobile};`
}

---

<BaseLayout
  title="Joner Football | Technique Above Everything"
  description="Joner Football. Elite football coaching, global camps, and the world's best coaching app. Technique above everything."
  canonical="https://jonerfootball.com/"
>
  <Fragment slot="head">
    <style>
      .story-page {
        background: #0A0A0A;
        color: #FFFFFF;
        overflow-x: clip;
      }

      .story-shell {
        --story-progress: 0;
        --growth-progress: 0;
        --hero-word-progress: 0;
        --ball-rotation: 0deg;
        --ball-size: clamp(4.8rem, 5.8vw, 5.8rem);
        --ball-opacity: 1;
        --mask-x: var(--marker-x);
        --mask-y: var(--marker-y);
      }

      .story-viewport {
        height: calc(100svh - 4rem);
        min-height: 44rem;
        color: #FFFFFF;
        background: #0A0A0A;
        contain: layout paint;
        transition:
          background-color 700ms cubic-bezier(.22,.61,.36,1),
          color 700ms cubic-bezier(.22,.61,.36,1);
      }

      .story-bg {
        opacity: 0;
        transform: scale(1.06);
        object-position: var(--story-object-desktop, 50% 50%);
        filter: grayscale(0.12) contrast(1.06) brightness(0.76) saturate(1.04);
        transition:
          opacity 800ms cubic-bezier(.22,.61,.36,1),
          transform 1100ms cubic-bezier(.22,.61,.36,1),
          filter 900ms ease;
      }

      .story-bg[data-state="active"] {
        opacity: 1;
        transform: scale(1);
        filter: grayscale(0.02) contrast(1.08) brightness(0.92) saturate(1.06);
      }

      .story-bg[data-media="video"][data-state="active"] {
        opacity: 0.96;
        filter: grayscale(0) contrast(1.06) brightness(0.92) saturate(1.04);
      }

      .story-shade {
        opacity: 1;
        transition: opacity 700ms ease;
      }

      .story-shade-main {
        background: rgba(0,0,0,0.38);
      }

      .story-shade-bottom {
        background: linear-gradient(to top, rgba(10,10,10,0.82), rgba(10,10,10,0.3), rgba(10,10,10,0));
      }

      .story-vignette {
        box-shadow: inset 0 0 160px rgba(0,0,0,0.58);
      }

      .story-kicker {
        letter-spacing: 0.26em;
      }

      .story-rail {
        color: rgba(255,255,255,0.54);
        transition: color 700ms ease, opacity 420ms ease;
      }

      .story-rail-line {
        background: rgba(255,255,255,0.16);
        transition: background 700ms ease;
      }

      .story-rail-fill {
        height: calc(var(--story-progress) * 100%);
        background: #E30613;
      }

      .story-rail-dot,
      .story-rail-label {
        transition:
          opacity 260ms ease,
          transform 260ms ease,
          color 260ms ease,
          border-color 260ms ease,
          background-color 260ms ease;
      }

      .story-rail-label {
        display: block;
        letter-spacing: 0.14em;
        line-height: 1;
      }

      .story-rail-number,
      .story-rail-name {
        display: block;
      }

      .story-rail-number {
        color: rgba(255,255,255,0.88);
        font-size: 0.78rem;
      }

      .story-rail-name {
        margin-top: 0.28rem;
        color: rgba(255,255,255,0.72);
        font-size: 0.72rem;
      }

      .story-rail-dot[data-state="active"] {
        background: #E30613;
        border-color: #E30613;
        opacity: 1;
        transform: scale(1.18);
      }

      .story-rail-dot[data-state="inactive"] {
        opacity: 0.34;
      }

      .story-rail-label[data-state="active"] {
        opacity: 1;
        transform: translateX(0);
      }

      .story-rail-label[data-state="active"] .story-rail-number,
      .story-rail-label[data-state="active"] .story-rail-name {
        color: #FFFFFF;
      }

      .story-rail-label[data-state="inactive"] {
        opacity: 0.42;
        transform: translateX(-0.18rem);
      }

      .story-copy {
        display: none;
        position: absolute;
        max-width: 34rem;
        opacity: 0;
        pointer-events: none;
      }

      .story-copy[data-state="active"] {
        display: block;
        opacity: 1;
        pointer-events: auto;
        animation: storyCopyIn 620ms cubic-bezier(.22,.61,.36,1) both;
      }

      .story-copy[data-pos="center"] {
        left: 50%;
        top: 20%;
        transform: translateX(-50%);
        text-align: center;
        max-width: 56rem;
      }

      .story-copy[data-pos="action"] {
        left: 50%;
        top: 42%;
        transform: translateX(-50%);
        text-align: center;
        max-width: 56rem;
      }

      .story-copy[data-pos="action-left"] {
        left: clamp(13rem, 16vw, 15rem);
        top: 7vh;
        max-width: 68rem;
        text-align: left;
      }

      .story-copy[data-pos="hidden"] {
        display: none !important;
      }

      .story-copy[data-pos="left"] {
        left: clamp(13rem, 18vw, 18rem);
        top: 26%;
      }

      .story-copy[data-pos="global-bottom"] {
        left: clamp(13rem, 18vw, 18rem);
        bottom: 11vh;
      }

      .story-copy[data-pos="top-left"] {
        left: clamp(13rem, 18vw, 18rem);
        top: 18vh;
      }

      .story-copy[data-pos="right"] {
        right: clamp(3rem, 8vw, 9rem);
        top: 26%;
      }

      .story-copy[data-pos="mid-left"] {
        left: clamp(16rem, 26vw, 25rem);
        top: 26%;
      }

      .story-copy[data-pos="hq-left"] {
        left: clamp(13rem, 18vw, 18rem);
        top: 43vh;
      }

      .story-stamp {
        display: inline-flex;
        margin-top: 1.15rem;
        border-left: 2px solid #E30613;
        padding-left: 1rem;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(2.8rem, 5.6vw, 5rem);
        line-height: 0.92;
        text-transform: uppercase;
        color: #FFFFFF;
      }

      @keyframes storyCopyIn {
        from { opacity: 0; transform: translateY(1.25rem); }
        to { opacity: 1; transform: translateY(0); }
      }

      .story-copy[data-pos="center"][data-state="active"] {
        animation-name: storyCopyInCenter;
      }

      .story-copy[data-pos="action"][data-state="active"] {
        animation-name: storyCopyInCenter;
      }

      @keyframes storyCopyInCenter {
        from { opacity: 0; transform: translate(-50%, 1.25rem); }
        to { opacity: 1; transform: translate(-50%, 0); }
      }

      .story-ball-system {
        left: var(--marker-x);
        top: var(--marker-y);
        opacity: var(--ball-opacity);
        transition:
          left 320ms cubic-bezier(.22,.61,.36,1),
          top 320ms cubic-bezier(.22,.61,.36,1),
          opacity 400ms ease;
        will-change: left, top, opacity;
      }

      .story-photo-ball-mask {
        display: none;
        left: var(--mask-x);
        top: var(--mask-y);
        width: clamp(0rem, var(--mask-size), 14rem);
        height: clamp(0rem, var(--mask-size), 14rem);
        transform: translate(-50%, -50%);
        border-radius: 999px;
        background:
          radial-gradient(circle, rgba(0,0,0,1) 0 76%, rgba(0,0,0,0.84) 86%, rgba(0,0,0,0) 100%);
        filter: blur(0.28rem);
        opacity: var(--mask-opacity, 0.92);
        pointer-events: none;
        transition:
          left 180ms linear,
          top 180ms linear,
          opacity 380ms ease,
          width 180ms linear,
          height 180ms linear;
        will-change: left, top;
      }

      .story-ball-pulse {
        width: var(--ball-size);
        height: var(--ball-size);
        border-radius: 999px;
        position: relative;
        overflow: visible;
        border: 0;
        background: transparent;
        box-shadow:
          0 1.1rem 2rem rgba(0,0,0,0.4),
          0 0 2.8rem rgba(227,6,19,0.18);
        transform: translate(-50%, -50%) scale(var(--ball-fly-scale, 1)) rotateY(var(--ball-fly-rotate, 0deg));
        transition: border-color 700ms ease, box-shadow 700ms ease, transform 320ms cubic-bezier(.22,.61,.36,1);
      }

      .story-ball-pulse::before,
      .story-ball-pulse::after {
        display: none;
      }

      .story-ball-image {
        display: block;
        width: 100%;
        height: 100%;
        object-fit: contain;
        filter:
          saturate(1.18)
          contrast(1.04)
          drop-shadow(0 1.1rem 1.35rem rgba(0,0,0,0.48));
        animation: storyBallSpinFlat 1.35s linear infinite;
        transform-origin: 50% 50%;
        will-change: transform;
      }

      @keyframes storyBallSpinFlat {
        from { transform: rotate(0deg); }
        to { transform: rotate(360deg); }
      }

      .story-ball-logo {
        display: none;
      }

      .story-mini-ball {
        position: absolute;
        border-radius: 999px;
        background: url('/images/home-storyboard/yellow-football-object.png') center / contain no-repeat;
        box-shadow:
          0 0 1.8rem rgba(227,6,19,0.22);
      }

      .hero-scroll-title {
        display: grid;
        gap: 0.02em;
      }

      .hero-word {
        display: block;
        opacity: 0;
        transform: translateY(1.2rem);
        transition:
          opacity 360ms cubic-bezier(.22,.61,.36,1),
          transform 520ms cubic-bezier(.22,.61,.36,1);
      }

      .hero-word[data-visible="true"] {
        opacity: 1;
        transform: translateY(0);
      }

      .story-overlay {
        display: none;
        opacity: 0;
        transition: opacity 500ms ease;
        pointer-events: none;
      }

      .story-overlay a {
        pointer-events: auto;
      }

      .story-overlay[data-state="active"] {
        display: block;
        opacity: 1;
        pointer-events: auto;
        animation: storyOverlayIn 720ms cubic-bezier(.22,.61,.36,1) both;
      }

      @keyframes storyOverlayIn {
        from { opacity: 0; transform: translateY(1rem); }
        to { opacity: 1; transform: translateY(0); }
      }

      .story-overlay--choose::before {
        content: "";
        position: absolute;
        inset: 0;
        background:
          linear-gradient(90deg, rgba(0,0,0,0.55) 0%, rgba(0,0,0,0.32) 55%, rgba(0,0,0,0.18) 100%);
        pointer-events: none;
        z-index: 0;
      }

      .story-overlay-choose {
        position: absolute;
        inset: 0;
        z-index: 5;
        display: flex;
        align-items: center;
        padding-top: 6vh;
        padding-bottom: 8vh;
        padding-left: clamp(13rem, 18vw, 18rem);
        padding-right: clamp(3rem, 6vw, 6rem);
      }

      .story-overlay-choose > * {
        max-width: 78rem;
        width: 100%;
      }

      .story-mobile-choose {
        margin-top: 1.5rem;
      }

      .metric-card,
      .path-card,
      .primary-path-card,
      .global-proof-card,
      .phone-shell {
        border: 1px solid rgba(255,255,255,0.16);
        background: rgba(10,10,10,0.46);
        backdrop-filter: blur(18px);
      }

      .primary-path-card {
        min-height: 7.4rem;
      }

      .action-path-panel {
        left: clamp(13rem, 16vw, 15rem);
        top: 34vh;
        width: min(64vw, 70rem);
      }

      .action-path-grid {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.75rem 1rem;
      }

      .primary-path-card {
        position: relative;
        overflow: hidden;
        border-color: rgba(255,255,255,0.34);
        background: rgba(5,5,5,0.42);
      }

      .primary-path-card::before,
      .primary-path-card::after {
        content: "";
        position: absolute;
        pointer-events: none;
      }

      .primary-path-card::before {
        left: 0;
        top: 0;
        width: 3.7rem;
        height: 1.25rem;
        border-left: 2px solid #E30613;
        border-top: 2px solid #E30613;
        clip-path: polygon(0 0, 100% 0, 76% 100%, 0 100%);
      }

      .primary-path-card::after {
        right: 0;
        bottom: 0;
        width: 4.6rem;
        height: 1.1rem;
        border-right: 2px solid rgba(255,255,255,0.7);
        border-bottom: 2px solid #E30613;
      }

      .phone-shell {
        border-radius: 2rem;
        padding: 0.55rem;
        box-shadow: 0 2rem 5rem rgba(0,0,0,0.18);
      }

      .phone-screen {
        border-radius: 1.45rem;
      }

      .story-cta-row {
        margin-top: 1.5rem;
        display: flex;
        flex-wrap: wrap;
        gap: 0.65rem;
        max-width: 34rem;
      }

      .story-copy[data-pos="center"] .story-cta-row,
      .story-copy[data-pos="action"] .story-cta-row {
        margin-left: auto;
        margin-right: auto;
        justify-content: center;
      }

      .story-cta-link {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        min-width: 9.8rem;
        min-height: 3.75rem;
        border: 1px solid rgba(255,255,255,0.34);
        background: rgba(5,5,5,0.72);
        color: #FFFFFF;
        padding: 0.95rem 1.15rem;
        font-family: 'Anton', Impact, sans-serif;
        font-size: 0.82rem;
        letter-spacing: 0.16em;
        text-transform: uppercase;
        line-height: 1;
        text-align: center;
        backdrop-filter: blur(16px);
        text-shadow: 0 1px 10px rgba(0,0,0,0.6);
        transition: border-color 180ms ease, background-color 180ms ease, color 180ms ease, transform 180ms ease;
      }

      .story-cta-link:hover {
        border-color: #E30613;
        background: #E30613;
        color: #FFFFFF;
        transform: scale(1.045);
      }

      .story-cta-row--mobile {
        display: none;
      }

      .story-cta-link.primary,
      .story-cta-link.red,
      .story-copy:not([data-pos="center"]) .story-cta-link:not(.store-badge) {
        border-color: rgba(255,255,255,0.34);
        background: rgba(5,5,5,0.72);
      }

      .story-cta-link.primary:hover,
      .story-cta-link.red:hover,
      .story-copy:not([data-pos="center"]) .story-cta-link:not(.store-badge):hover {
        border-color: #E30613;
        background: #E30613;
      }

      .store-badge {
        gap: 0.55rem;
      }

      .store-badge svg {
        width: 1.05rem;
        height: 1.05rem;
        flex: 0 0 auto;
      }

      .store-badge span {
        display: inline-flex;
        align-items: center;
        justify-content: center;
      }

      .story-copy[data-index="5"] {
        left: 50%;
        right: auto;
        width: min(62rem, 78vw);
        max-width: none;
        transform: translateX(-50%);
        text-align: center;
      }

      .story-copy[data-index="5"] .story-body {
        max-width: 46rem;
        margin-left: auto;
        margin-right: auto;
        text-align: center;
      }

      .story-copy[data-index="5"] .story-cta-row {
        max-width: none;
        justify-content: center;
        align-items: stretch;
      }

      .story-copy[data-index="5"] .story-cta-link {
        width: 9.8rem;
        height: 4.1rem;
        min-width: 9.8rem;
        min-height: 4.1rem;
        padding: 0;
        text-align: center;
      }

      .story-copy[data-index="5"] .store-badge {
        flex-direction: row;
        gap: 0.48rem;
      }

      .story-copy[data-index="5"] .store-badge svg {
        width: 1rem;
        height: 1rem;
      }

      .story-copy[data-index="5"] .store-badge span {
        line-height: 1;
        text-align: center;
      }

      .growth-bar {
        transform: scaleY(calc(0.18 + (var(--growth-progress) * 0.82)));
        transform-origin: bottom;
        transition: transform 120ms linear;
      }

      .growth-link-row {
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.5rem;
      }

      .story-shell[data-active="7"] .story-bg[data-state="active"] {
        filter: grayscale(0) contrast(1.06) brightness(1) saturate(1.05);
      }

      .story-shell[data-active="7"] .story-shade-main {
        background:
          linear-gradient(90deg, rgba(0,0,0,0.86) 0%, rgba(0,0,0,0.56) 42%, rgba(0,0,0,0.2) 72%),
          rgba(0,0,0,0.22);
      }

      .story-shell[data-active="7"] .story-shade-bottom {
        opacity: 0.34;
      }

      .story-shell[data-active="7"] .story-vignette {
        box-shadow: inset 0 0 80px rgba(0,0,0,0.22);
      }

      .action-intro {
        position: absolute;
        z-index: 45;
        left: clamp(13rem, 18vw, 18rem);
        top: 21vh;
        max-width: 44rem;
      }

      .action-intro h2 {
        color: #FFFFFF;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(3.8rem, 7.2vw, 7rem);
        line-height: 0.9;
        letter-spacing: 0.045em;
        text-transform: uppercase;
        text-shadow: 0 1.4rem 3rem rgba(0,0,0,0.62);
      }

      .action-intro p {
        margin-top: 1rem;
        max-width: 31rem;
        color: rgba(255,255,255,0.76);
        font-size: 1.08rem;
        line-height: 1.55;
      }

      .action-board {
        position: absolute;
        z-index: 45;
        left: clamp(13rem, 18vw, 18rem);
        right: clamp(3rem, 6vw, 6rem);
        bottom: 8vh;
        height: min(34vh, 22rem);
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        grid-template-rows: repeat(3, minmax(0, 1fr));
        gap: 0.75rem;
      }

      .action-card {
        position: relative;
        display: grid;
        grid-template-columns: auto 1fr auto;
        align-items: center;
        gap: clamp(0.55rem, 1vw, 0.9rem);
        min-width: 0;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.36);
        border-radius: 0.62rem;
        background:
          linear-gradient(135deg, rgba(255,255,255,0.18), rgba(255,255,255,0.04)),
          rgba(6,6,6,0.58);
        color: #FFFFFF;
        padding: clamp(0.7rem, 1.05vw, 1rem);
        text-decoration: none;
        box-shadow:
          inset 0 1px 0 rgba(255,255,255,0.22),
          0 1rem 2.4rem rgba(0,0,0,0.24);
        backdrop-filter: blur(18px) saturate(1.22);
        transform: translateZ(0);
        transition:
          transform 220ms cubic-bezier(.22,1,.36,1),
          border-color 220ms ease,
          background-color 220ms ease,
          box-shadow 220ms ease;
      }

      .action-card::before,
      .action-card::after {
        content: "";
        position: absolute;
        pointer-events: none;
      }

      .action-card::before {
        inset: 0;
        background: radial-gradient(circle at 18% 0%, rgba(227,6,19,0.2), transparent 42%);
        opacity: 0;
        transition: opacity 220ms ease;
      }

      .action-card::after {
        left: 0;
        top: 0;
        width: 42%;
        height: 2px;
        background: linear-gradient(90deg, #E30613, rgba(227,6,19,0));
        opacity: 0.9;
      }

      .action-card:hover,
      .action-card:focus-visible {
        border-color: rgba(255,255,255,0.82);
        background-color: rgba(12,12,12,0.78);
        box-shadow:
          inset 0 1px 0 rgba(255,255,255,0.28),
          0 1.4rem 3rem rgba(0,0,0,0.34),
          0 0 2.2rem rgba(227,6,19,0.22);
        outline: none;
        transform: translateY(-0.18rem) scale(1.018);
      }

      .action-card:hover::before,
      .action-card:focus-visible::before {
        opacity: 1;
      }

      .action-card:active {
        transform: translateY(0) scale(0.985);
      }

      .action-card-icon {
        position: relative;
        display: grid;
        place-items: center;
        width: clamp(2rem, 2.35vw, 2.65rem);
        height: clamp(2rem, 2.35vw, 2.65rem);
        border: 1px solid rgba(227,6,19,0.78);
        border-radius: 999px;
        background: rgba(227,6,19,0.16);
        color: #FFFFFF;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(0.62rem, 0.76vw, 0.78rem);
        letter-spacing: 0.04em;
        line-height: 1;
        box-shadow: 0 0 1.3rem rgba(227,6,19,0.14);
      }

      .action-card-copy {
        position: relative;
        min-width: 0;
      }

      .action-card-eyebrow {
        display: block;
        color: rgba(255,255,255,0.58);
        font-family: Arial, sans-serif;
        font-size: clamp(0.55rem, 0.72vw, 0.68rem);
        font-weight: 700;
        letter-spacing: 0.14em;
        line-height: 1;
        text-transform: uppercase;
      }

      .action-card-title {
        display: block;
        margin-top: 0.28rem;
        color: #FFFFFF;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(0.98rem, 1.34vw, 1.42rem);
        letter-spacing: 0.045em;
        line-height: 0.95;
        text-transform: uppercase;
        text-shadow: 0 0.3rem 1.2rem rgba(0,0,0,0.42);
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
      }

      .action-card-arrow {
        position: relative;
        color: #E30613;
        font-family: Arial, sans-serif;
        font-size: 1.25rem;
        line-height: 1;
        transition: transform 220ms cubic-bezier(.22,1,.36,1), color 220ms ease;
      }

      .action-card:hover .action-card-arrow,
      .action-card:focus-visible .action-card-arrow {
        color: #FFFFFF;
        transform: translateX(0.18rem);
      }

      .journey-hotspot {
        position: absolute;
        z-index: 45;
        display: block;
        border-radius: 0.45rem;
        background: rgba(227,6,19,0);
        border: 1px solid rgba(227,6,19,0);
        transition:
          background-color 180ms ease,
          border-color 180ms ease,
          box-shadow 180ms ease;
      }

      .journey-hotspot:hover,
      .journey-hotspot:focus-visible {
        background: rgba(227,6,19,0.1);
        border-color: rgba(227,6,19,0.85);
        box-shadow: 0 0 2rem rgba(227,6,19,0.22);
        outline: none;
      }

      .journey-section {
        position: relative;
        min-height: calc(100svh - 4rem);
        overflow: hidden;
        background: #030303;
      }

      .journey-section img {
        display: block;
        width: 100%;
        min-height: calc(100svh - 4rem);
        object-fit: cover;
        object-position: left center;
      }

      .journey-hotspot.juniors { left: 18%; top: 46%; width: 17%; height: 13%; }
      .journey-hotspot.sixEight { left: 37.5%; top: 46%; width: 18.5%; height: 13%; }
      .journey-hotspot.performance { left: 58%; top: 43.5%; width: 35%; height: 20%; }
      .journey-hotspot.start { left: 77%; top: 84.4%; width: 18.5%; height: 8.2%; border-radius: 0.7rem; }

      .home-video-section {
        background:
          radial-gradient(circle at 18% 0%, rgba(227,6,19,0.18), transparent 32rem),
          #050505;
        color: #FFFFFF;
        padding: clamp(4rem, 7vw, 6rem) 1.5rem clamp(3.5rem, 6vw, 5rem);
      }

      .home-video-inner {
        width: min(1100px, 92vw);
        margin: 0 auto;
      }

      .home-video-grid {
        margin-top: 2rem;
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: clamp(1rem, 2vw, 1.5rem);
      }

      .home-video-card h3 {
        margin: 0 0 0.85rem;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(1.7rem, 2.8vw, 2.7rem);
        line-height: 0.95;
        letter-spacing: 0.04em;
        text-transform: uppercase;
      }

      .home-video-frame {
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(255,255,255,0.18);
        background: #000;
        aspect-ratio: 16 / 9;
        box-shadow: 0 2rem 5rem rgba(0,0,0,0.38);
      }

      @media (max-width: 767px) {
        .home-video-grid { grid-template-columns: 1fr; }
      }

      .home-video-frame iframe {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        border: 0;
      }

      .global-proof-card {
        box-shadow: 0 2rem 5rem rgba(0,0,0,0.22);
      }

      .keyframe-card {
        min-height: 26rem;
      }

      .story-frame-bg {
        object-position: var(--story-object-desktop, 50% 50%);
      }



      .story-mobile {
        --mobile-story-progress: 0;
        display: none;
        position: relative;
        overflow: clip;
        background: #050505;
        color: #FFFFFF;
      }

      .story-mobile-global-rail {
        position: absolute;
        left: 1rem;
        top: 5.8rem;
        bottom: 5.2rem;
        z-index: 20;
        width: 1.5rem;
        pointer-events: none;
      }

      .story-mobile-global-line {
        position: absolute;
        left: 0.72rem;
        top: 0;
        bottom: 0;
        width: 1px;
        background: rgba(255,255,255,0.24);
      }

      .story-mobile-global-line::before,
      .story-mobile-global-line::after {
        content: "+";
        position: absolute;
        left: -0.25rem;
        color: rgba(255,255,255,0.76);
        font-family: Arial, sans-serif;
        font-size: 1rem;
        line-height: 1;
      }

      .story-mobile-global-line::before { top: -0.9rem; }
      .story-mobile-global-line::after { bottom: -0.9rem; }

      .story-mobile-global-fill {
        position: absolute;
        left: 0.72rem;
        top: 0;
        width: 1px;
        height: calc(var(--mobile-story-progress) * 100%);
        background: #E30613;
        box-shadow: 0 0 1rem rgba(227,6,19,0.52);
      }

      .story-mobile-global-dots {
        position: absolute;
        left: 0.48rem;
        top: 0;
        bottom: 0;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .story-mobile-global-dots span {
        width: 0.5rem;
        height: 0.5rem;
        border-radius: 999px;
        border: 1px solid rgba(255,255,255,0.38);
        background: rgba(0,0,0,0.35);
      }

      .story-mobile-panel {
        position: relative;
        min-height: calc(100svh - 4rem);
        overflow: hidden;
        display: grid;
        place-items: center;
        padding: 5.5rem 1.35rem 4.5rem 2.5rem;
        isolation: isolate;
        scroll-snap-align: start;
      }

      .story-mobile-bg,
      .story-mobile-bg img {
        position: absolute;
        inset: 0;
        width: 100%;
        height: 100%;
        z-index: -3;
      }

      .story-mobile-bg,
      .story-mobile-bg img {
        object-fit: cover;
        object-position: var(--story-object-mobile, 62% 50%);
      }

      .story-mobile-panel:nth-of-type(1) .story-mobile-bg img {
        object-position: 0% 50%;
        transform: scale(0.88) translateX(13%);
        transform-origin: 50% 50%;
      }

      .story-mobile-panel:nth-of-type(2) .story-mobile-bg img {
        object-position: 0% 50%;
        transform: scale(0.9) translateX(10%);
        transform-origin: 50% 50%;
      }

      .story-mobile-panel:nth-of-type(7) .story-mobile-bg img {
        object-position: 78% 50%;
        transform: scale(0.88);
        transform-origin: 78% 50%;
      }

      .story-mobile-panel::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: -2;
        background:
          linear-gradient(180deg, rgba(0,0,0,0.62), rgba(0,0,0,0.24) 42%, rgba(0,0,0,0.72)),
          radial-gradient(circle at 50% 36%, rgba(0,0,0,0.1), rgba(0,0,0,0.62) 74%);
      }

      .story-mobile-content {
        width: min(21rem, 82vw);
        margin: 0 auto;
        text-align: center;
      }

      .story-mobile-ball {
        position: absolute;
        right: clamp(1rem, 4vw, 1.7rem);
        bottom: clamp(1.5rem, 7vh, 4rem);
        z-index: 3;
        width: clamp(3.3rem, 17vw, 4.9rem);
        height: clamp(3.3rem, 17vw, 4.9rem);
        opacity: 0.92;
        pointer-events: none;
      }

      .story-mobile-panel:nth-of-type(1) .story-mobile-ball {
        left: clamp(1rem, 5vw, 1.7rem);
        right: auto;
        bottom: clamp(4.1rem, 10vh, 5.6rem);
        width: clamp(3.1rem, 15vw, 4.3rem);
        height: clamp(3.1rem, 15vw, 4.3rem);
      }

      .story-mobile-panel:nth-of-type(2) .story-mobile-ball {
        left: clamp(1rem, 5vw, 1.7rem);
        right: auto;
        bottom: clamp(4.6rem, 11vh, 6rem);
        width: clamp(3.1rem, 15vw, 4.3rem);
        height: clamp(3.1rem, 15vw, 4.3rem);
      }

      .story-mobile-ball::before {
        content: "";
        position: absolute;
        inset: -0.65rem;
        border-radius: 999px;
        background: radial-gradient(circle, rgba(227,6,19,0.24), transparent 66%);
        filter: blur(0.2rem);
      }

      .story-mobile-ball img {
        position: relative;
        width: 100%;
        height: 100%;
        object-fit: contain;
        animation: storyBallSpinFlat 1.8s linear infinite;
        filter: drop-shadow(0 1rem 1.3rem rgba(0,0,0,0.45));
      }

      .story-mobile-kicker {
        margin-bottom: 1.05rem;
        color: rgba(255,255,255,0.76);
        font-family: Arial, sans-serif;
        font-size: 0.72rem;
        font-weight: 900;
        letter-spacing: 0.18em;
        text-transform: uppercase;
      }

      .story-mobile-title {
        color: #FFFFFF;
        font-family: 'Anton', Impact, sans-serif;
        font-size: clamp(3rem, 13vw, 4.9rem);
        line-height: 0.92;
        letter-spacing: 0.035em;
        text-transform: uppercase;
        text-shadow: 0 1.2rem 2.8rem rgba(0,0,0,0.62);
      }

      .story-mobile-body {
        margin: 1.15rem auto 0;
        max-width: 20rem;
        color: rgba(255,255,255,0.78);
        font-size: 1rem;
        line-height: 1.48;
      }

      .story-mobile-rail {
        display: none !important;
        position: absolute;
        left: 0.95rem;
        top: 7.8rem;
        bottom: 4.8rem;
        z-index: 4;
        display: grid;
        grid-template-columns: 0.8rem auto;
        gap: 0.62rem;
        align-items: stretch;
        pointer-events: none;
      }

      .story-mobile-line {
        position: relative;
        width: 1px;
        margin-left: 0.38rem;
        background: rgba(255,255,255,0.28);
      }

      .story-mobile-line::before,
      .story-mobile-line::after {
        content: "+";
        position: absolute;
        left: -0.25rem;
        color: rgba(255,255,255,0.76);
        font-family: Arial, sans-serif;
        font-size: 1rem;
        line-height: 1;
      }

      .story-mobile-line::before { top: -0.9rem; }
      .story-mobile-line::after { bottom: -0.9rem; }

      .story-mobile-dot-list {
        position: absolute;
        left: 0.16rem;
        top: 0;
        bottom: 0;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
      }

      .story-mobile-dot {
        width: 0.48rem;
        height: 0.48rem;
        border-radius: 999px;
        border: 1px solid rgba(255,255,255,0.35);
        background: rgba(0,0,0,0.18);
      }

      .story-mobile-dot.active {
        border-color: #E30613;
        background: #E30613;
        box-shadow: 0 0 1rem rgba(227,6,19,0.45);
      }

      .story-mobile-labels {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        padding: 0.05rem 0;
      }

      .story-mobile-label {
        visibility: hidden;
        color: rgba(255,255,255,0.34);
        font-family: 'Anton', Impact, sans-serif;
        font-size: 0.62rem;
        letter-spacing: 0.1em;
        line-height: 1;
        text-transform: uppercase;
        white-space: nowrap;
      }

      .story-mobile-label.active {
        visibility: visible;
        color: #FFFFFF;
        text-shadow: 0 0.8rem 1.8rem rgba(0,0,0,0.72);
      }

      .story-mobile-cta-row {
        margin-top: 1.45rem;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 0.62rem;
      }

      .story-mobile-cta {
        border: 1px solid rgba(255,255,255,0.34);
        border-radius: 999px;
        background: rgba(5,5,5,0.56);
        color: #FFFFFF;
        padding: 0.78rem 1rem;
        font-family: 'Anton', Impact, sans-serif;
        font-size: 0.78rem;
        letter-spacing: 0.13em;
        line-height: 1;
        text-transform: uppercase;
        text-decoration: none;
        backdrop-filter: blur(14px);
      }

      .story-mobile-cta.primary {
        border-color: rgba(227,6,19,0.75);
        background: rgba(227,6,19,0.78);
      }

      .story-mobile-action-grid {
        margin-top: 1.35rem;
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 0.62rem;
      }

      .story-mobile-action {
        border: 1px solid rgba(255,255,255,0.28);
        border-radius: 0.82rem;
        background: rgba(5,5,5,0.58);
        color: #FFFFFF;
        padding: 0.82rem 0.7rem;
        font-family: 'Anton', Impact, sans-serif;
        font-size: 0.82rem;
        letter-spacing: 0.08em;
        line-height: 1;
        text-transform: uppercase;
        text-decoration: none;
        backdrop-filter: blur(14px);
      }

      .story-mobile-stats {
        margin: 1.1rem auto 0;
        display: grid;
        grid-template-columns: repeat(3, minmax(0, 1fr));
        gap: 0.48rem;
        width: min(20rem, 100%);
      }

      .story-mobile-stat {
        border: 1px solid rgba(255,255,255,0.2);
        background: rgba(5,5,5,0.48);
        padding: 0.55rem 0.35rem;
        text-align: center;
        backdrop-filter: blur(12px);
      }

      .story-mobile-stat strong {
        display: block;
        color: #fff;
        font-family: 'Anton', Impact, sans-serif;
        font-size: 1.2rem;
        line-height: 1;
      }

      .story-mobile-stat span {
        display: block;
        margin-top: 0.25rem;
        color: rgba(255,255,255,0.62);
        font-family: Arial, sans-serif;
        font-size: 0.5rem;
        font-weight: 900;
        letter-spacing: 0.12em;
        text-transform: uppercase;
      }

      .story-mobile-choose :global(.next-step-section),
      .story-mobile-choose :global(section) {
        padding: 0 !important;
        background: transparent !important;
      }

      .story-mobile-choose :global(h2) {
        font-size: clamp(2rem, 10vw, 3rem) !important;
      }

      .story-mobile-choose :global(a) {
        min-height: 3.15rem !important;
        padding: 0.7rem 0.65rem !important;
        font-size: 0.72rem !important;
      }

      @media (max-width: 767px) {
        .jfp-journey {
          display: none !important;
        }
      }

      @media (prefers-reduced-motion: reduce) {
        .story-bg,
        .story-copy,
        .story-overlay,
        .story-ball-system,
        .story-ball-image,
        .story-rail-dot,
        .story-rail-label {
          animation: none !important;
          transition: none !important;
        }
      }

      @media (max-width: 1100px) {
        .story-bg,
        .story-frame-bg {
          object-position: var(--story-object-tablet, var(--story-object-desktop, 50% 50%));
        }
      }

      @media (max-width: 767px) {
        #storyboard-desktop {
          display: none;
        }

        .story-mobile {
          display: block;
          scroll-snap-type: y proximity;
        }

        .story-viewport {
          height: calc(100svh - 4rem);
          min-height: 34rem;
        }

        .story-rail {
          display: block;
          left: 1rem;
          top: 12vh;
          width: 1.1rem;
          height: 66vh;
          opacity: 0.82;
        }

        .story-rail-line,
        .story-rail-fill {
          left: 0.5rem;
        }

        .story-rail .grid {
          grid-template-columns: 1rem;
          gap: 0;
        }

        .story-rail-dot {
          width: 0.42rem;
          height: 0.42rem;
          margin-left: 0.29rem;
        }

        .story-rail-label {
          display: none;
        }

        .story-bg,
        .story-frame-bg {
          object-position: var(--story-object-mobile, var(--story-object-tablet, var(--story-object-desktop, 50% 50%)));
        }

        .story-copy,
        .story-copy[data-pos="left"],
        .story-copy[data-pos="right"],
        .story-copy[data-pos="mid-left"],
        .story-copy[data-pos="top-left"],
        .story-copy[data-pos="hq-left"],
        .story-copy[data-pos="center"],
        .story-copy[data-pos="action"],
        .story-copy[data-pos="action-left"] {
          left: 2rem;
          right: 2rem;
          top: 18%;
          max-width: none;
          text-align: center;
          transform: none;
        }

        .story-copy[data-pos="center"][data-state="active"],
        .story-copy[data-pos="action"][data-state="active"],
        .story-copy[data-pos="action-left"][data-state="active"] {
          animation-name: storyCopyIn;
        }

        .story-copy[data-pos="center"] {
          left: 1.65rem;
          right: 1.65rem;
          top: 28%;
          text-align: center;
        }

        .story-copy[data-pos="action"] {
          top: 13%;
          text-align: center;
        }

        .story-copy[data-pos="action-left"] {
          top: 12%;
        }

        .story-copy[data-pos="hidden"] {
          display: none !important;
        }

        .story-copy-title.hero-scroll-title {
          font-size: clamp(2.65rem, 13.5vw, 4.2rem);
        }

        .story-copy-title:not(.hero-scroll-title) {
          font-size: clamp(2.15rem, 10.5vw, 3.4rem);
          line-height: 0.94;
        }

        .story-body {
          max-width: 20rem;
          margin-left: auto;
          margin-right: auto;
          font-size: 0.95rem;
          line-height: 1.48;
        }

        .story-copy[data-pos="right"] .story-body,
        .story-copy[data-pos="action"] .story-body,
        .story-copy[data-pos="action-left"] .story-body,
        .story-copy[data-pos="center"] .story-body {
          margin-left: auto;
          margin-right: auto;
        }

        .story-cta-row {
          max-width: 100%;
          justify-content: center;
        }

        .story-cta-row--mobile {
          display: flex;
          max-width: 20rem;
        }

        .story-ball-system {
          display: none;
        }

        .story-photo-ball-mask {
          display: none;
        }

        .story-bg {
          transform: none;
          transition: opacity 360ms ease, filter 360ms ease;
        }

        .story-bg[data-state="active"] {
          transform: none;
        }

        .story-overlay[data-state="active"] {
          display: none;
        }

        .story-overlay--action[data-state="active"] {
          display: block;
        }

        .action-board {
          left: 1rem;
          right: 1rem;
          top: auto;
          bottom: max(1rem, env(safe-area-inset-bottom));
          width: auto;
          height: auto;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          grid-template-rows: none;
          gap: 0.52rem;
        }

        .action-card {
          grid-template-columns: 1fr auto;
          gap: 0.35rem;
          min-height: 3.9rem;
          border-radius: 0.52rem;
          padding: 0.62rem;
          box-shadow:
            inset 0 1px 0 rgba(255,255,255,0.18),
            0 0.8rem 1.8rem rgba(0,0,0,0.28);
        }

        .action-card-icon {
          display: none;
        }

        .action-card-eyebrow {
          font-size: 0.48rem;
          letter-spacing: 0.1em;
        }

        .action-card-title {
          margin-top: 0.22rem;
          font-size: clamp(0.78rem, 3.4vw, 1rem);
          white-space: normal;
          line-height: 1;
        }

        .action-card-arrow {
          font-size: 1rem;
        }
      }
    </style>
  </Fragment>

  <main class="story-page">
    <section
      id="storyboard-desktop"
      class="story-shell relative block"
      data-story-shell
      data-active="0"
        style={`height: ${storyBeats.length * 82}vh; --marker-x: ${storyBeats[0].markerX}%; --marker-y: ${storyBeats[0].markerY}%; --mask-x: ${storyBeats[0].maskX ?? storyBeats[0].markerX}%; --mask-y: ${storyBeats[0].maskY ?? storyBeats[0].markerY}%; --mask-size: ${storyBeats[0].maskSize ?? 0}vw; --mask-opacity: ${(storyBeats[0].maskSize ?? 0) > 0 ? 0.98 : 0}; --ball-size: ${storyBeats[0].ballSize}; --ball-opacity: ${storyBeats[0].ballVisible === false ? 0 : 1};`}
    >
      <div class="story-viewport sticky top-16 overflow-hidden">
        {storyBeats.map((beat, index) => (
          beat.video ? (
            <video
              class="story-bg absolute inset-0 h-full w-full object-cover"
              aria-label={beat.alt}
              autoplay
              muted
              loop
              playsinline
              preload="metadata"
              poster={beat.image}
              style={mediaStyle(beat)}
              data-media="video"
              data-story-bg
              data-index={index}
              data-state={index === 0 ? 'active' : 'inactive'}
            >
              <source src={beat.video} type="video/mp4" />
            </video>
          ) : (
            <picture
              class="story-bg absolute inset-0 h-full w-full"
              data-media="image"
              data-story-bg
              data-index={index}
              data-state={index === 0 ? 'active' : 'inactive'}
            >
              {beat.mobileImage && <source media="(max-width: 767px)" srcset={beat.mobileImage} />}
              <img
                class="h-full w-full object-cover"
                src={beat.image}
                alt={beat.alt}
                loading={index === 0 ? 'eager' : 'lazy'}
                decoding="async"
                fetchpriority={index === 0 ? 'high' : 'auto'}
                style={mediaStyle(beat)}
              />
            </picture>
          )
        ))}

        <div class="story-shade story-shade-main absolute inset-0"></div>
        <div class="story-shade story-shade-bottom absolute inset-x-0 bottom-0 h-2/3"></div>
        <div class="story-vignette absolute inset-0"></div>

        <aside class="story-rail absolute left-8 top-[10vh] z-30 h-[66vh] w-44">
          <div class="story-rail-line absolute left-5 top-0 bottom-0 w-px"></div>
          <div class="story-rail-fill absolute left-5 top-0 w-px transition-[height] duration-150 ease-linear"></div>
          <div class="absolute inset-y-0 left-0 flex flex-col justify-between py-1">
            {storyBeats.map((beat, index) => (
              <div class="grid grid-cols-[2.5rem_1fr] items-center gap-3">
                <span class="story-rail-dot ml-[0.875rem] h-3 w-3 rounded-full border border-current bg-transparent" data-story-dot data-index={index} data-state={index === 0 ? 'active' : 'inactive'}></span>
                <span class="story-rail-label font-heading uppercase" data-story-label data-index={index} data-state={index === 0 ? 'active' : 'inactive'}>
                  <span class="story-rail-number">{beat.number}</span>
                  <span class="story-rail-name">{beat.label}</span>
                </span>
              </div>
            ))}
          </div>
        </aside>

        <div class="story-photo-ball-mask absolute z-10" data-story-mask aria-hidden="true"></div>

        <div class="story-ball-system absolute z-20" data-story-ball aria-hidden="true">
          <div class="story-ball-pulse">
            <img class="story-ball-image" src="/images/home-storyboard/yellow-football-object.png" alt="" />
          </div>
        </div>

        {storyBeats.map((beat, index) => (
          <article class="story-copy z-40" data-story-copy data-index={index} data-pos={beat.copyPos} data-state={index === 0 ? 'active' : 'inactive'}>
            {index !== 0 && (
              <p class="story-kicker font-heading uppercase text-joner-red text-xs mb-5">{beat.kicker || `${beat.number} / ${beat.label}`}</p>
            )}
            {index === 0 ? (
              <h1 class="story-copy-title hero-scroll-title font-heading uppercase leading-[0.9] text-white text-6xl xl:text-8xl" aria-label={beat.title}>
                <span class="hero-word" data-hero-word data-step="0">Technique</span>
                <span class="hero-word" data-hero-word data-step="1">Above</span>
                <span class="hero-word" data-hero-word data-step="2">Everything</span>
              </h1>
            ) : (
              <h2 class:list={["story-copy-title font-heading uppercase leading-[0.9] text-white", beat.copyPos === 'center' ? 'text-5xl xl:text-7xl' : 'text-4xl xl:text-6xl']}>
                {beat.title}
              </h2>
            )}
            {beat.body && (
              <p class="story-body font-body text-lg xl:text-xl text-white/76 leading-relaxed mt-6 max-w-[32rem]">{beat.body}</p>
            )}
            {beat.stamp && (
              <p class="story-stamp">{beat.stamp}</p>
            )}
            {beat.ctas && beat.ctaPlacement !== 'overlay' && (
              <div class="story-cta-row">
                {beat.ctas.map((cta, ctaIndex) => (
                  <a
                    class:list={["story-cta-link", cta.badge ? 'store-badge' : '']}
                    href={cta.href}
                    target={cta.external ? '_blank' : undefined}
                    rel={cta.external ? 'noopener' : undefined}
                  >
                    {cta.badge === 'apple' ? (
                      <><svg fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z" /></svg><span>{cta.label}</span></>
                    ) : cta.badge === 'google' ? (
                      <><svg fill="currentColor" viewBox="0 0 24 24" aria-hidden="true"><path d="M3 20.5v-17c0-.83.94-1.3 1.6-.8l14 8.5c.6.4.6 1.2 0 1.6l-14 8.5c-.66.5-1.6.03-1.6-.8z" /></svg><span>{cta.label}</span></>
                    ) : cta.label}
                  </a>
                ))}
              </div>
            )}
            {beat.ctas && beat.ctaPlacement === 'overlay' && (
              <div class="story-cta-row story-cta-row--mobile">
                {beat.ctas.map((cta) => (
                  <a
                    class="story-cta-link"
                    href={cta.href}
                    target={cta.external ? '_blank' : undefined}
                    rel={cta.external ? 'noopener' : undefined}
                  >
                    {cta.label}
                  </a>
                ))}
              </div>
            )}
          </article>
        ))}

        {storyBeats.map((beat, index) => (
          <div class:list={['story-overlay absolute inset-0 z-30', beat.overlay === 'chooseNextStep' && 'story-overlay--choose']} data-story-overlay data-index={index} data-state={index === 0 ? 'active' : 'inactive'} hidden={index !== 0}>
            {beat.overlay === 'growth' && (
              <div class="absolute right-[6vw] top-[15vh] w-[38vw] max-w-[40rem]">
                <div class="metric-card p-6">
                  <p class="story-kicker font-heading text-[0.68rem] uppercase text-[#E30613]">Social growth</p>
                  <div class="mt-4 flex items-end justify-between gap-4">
                    <p class="font-heading text-8xl leading-none text-white" data-growth-count data-growth-target={stats[0].target} data-growth-format={stats[0].format}>{stats[0].value}</p>
                    <div class="flex h-40 flex-1 items-end gap-2">
                      {[18, 32, 48, 66, 88, 100].map((height) => (
                        <span class="growth-bar block flex-1 bg-[#E30613]" style={`height: ${height}%; opacity: ${0.32 + height / 150};`}></span>
                      ))}
                    </div>
                  </div>
                  <div class="mt-5 grid grid-cols-3 gap-2">
                    {stats.slice(1).map((stat) => (
                      <div class="border border-white/12 bg-white/8 p-3">
                        <p class="font-heading text-2xl leading-none text-white" data-growth-count data-growth-target={stat.target} data-growth-format={stat.format}>{stat.value}</p>
                        <p class="story-kicker mt-2 font-heading text-[0.54rem] uppercase text-white/54">{stat.label}</p>
                      </div>
                    ))}
                  </div>
                </div>
                {beat.ctas && beat.ctaPlacement === 'overlay' && (
                  <div class="growth-link-row mt-5">
                    {beat.ctas.map((cta) => (
                      <a
                        class="story-cta-link text-center"
                        href={cta.href}
                        target={cta.external ? '_blank' : undefined}
                        rel={cta.external ? 'noopener' : undefined}
                      >
                        {cta.label}
                      </a>
                    ))}
                  </div>
                )}

              </div>
            )}

            {beat.overlay === 'chooseNextStep' && (
              <div class="story-overlay-choose">
                <ChooseNextStep />
              </div>
            )}

          </div>
        ))}
      </div>
    </section>

    <HomeStoryMobile />

    <section class="home-video-section">
      <div class="home-video-inner">
        <p class="story-kicker font-heading text-xs uppercase text-joner-red">Watch the work</p>
        <div class="home-video-grid">
          <article class="home-video-card">
            <h3>Inside Joner Football</h3>
            <div class="home-video-frame">
              <iframe
                src="https://www.youtube-nocookie.com/embed/axQYlKahQ58"
                title="Inside Joner Football"
                loading="lazy"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen
              ></iframe>
            </div>
          </article>
          <article class="home-video-card">
            <h3>Our Training</h3>
            <div class="home-video-frame">
              <iframe
                src="https://www.youtube-nocookie.com/embed/pvFy2YJsrZ0"
                title="Our Training"
                loading="lazy"
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                allowfullscreen
              ></iframe>
            </div>
          </article>
        </div>
      </div>
    </section>

  </main>

  <script is:inline>
    (function () {
      var beatMarkers = [
        { x: 24.2, y: 48, maskX: 24.2, maskY: 48, maskSize: 0, ballSize: 'clamp(6.1rem, 7.8vw, 7.2rem)', ballVisible: true },
        { x: 63.2, y: 81, maskX: 63.2, maskY: 81, maskSize: 0, ballSize: 'clamp(6.8rem, 8.4vw, 7.9rem)', ballVisible: true },
        { x: 37.2, y: 72, maskX: 37.2, maskY: 72, maskSize: 0, ballSize: 'clamp(5.8rem, 6.8vw, 6.6rem)', ballVisible: false },
        { x: 34, y: 72, maskX: 34, maskY: 72, maskSize: 0, ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)', ballVisible: false },
        { x: 24, y: 75, maskX: 24, maskY: 75, maskSize: 0, ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)', ballVisible: false },
        { x: 77, y: 72, maskX: 77, maskY: 72, maskSize: 0, ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)', ballVisible: false },
        { x: 35, y: 70, maskX: 35, maskY: 70, maskSize: 0, ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)', ballVisible: false },
        { x: 18, y: 77, maskX: 18, maskY: 77, maskSize: 0, ballSize: 'clamp(4.8rem, 5.8vw, 5.7rem)', ballVisible: false }
      ];

      function clamp(value, min, max) {
        return Math.min(Math.max(value, min), max);
      }

      function lerp(start, end, amount) {
        return start + (end - start) * amount;
      }

      function smoothstep(value) {
        return value * value * (3 - (2 * value));
      }

      function initMobileStoryProgress() {
        var mobileStory = document.querySelector('[data-mobile-story]');
        if (!mobileStory) return;

        function update() {
          var rect = mobileStory.getBoundingClientRect();
          var max = Math.max(1, rect.height - window.innerHeight);
          var progress = clamp((0 - rect.top) / max, 0, 1);
          mobileStory.style.setProperty('--mobile-story-progress', progress.toFixed(4));
        }

        update();
        window.addEventListener('scroll', update, { passive: true });
        window.addEventListener('resize', update);
      }

      function initDesktopStory() {
        var shell = document.querySelector('[data-story-shell]');
        if (!shell || window.matchMedia('(max-width: 767px)').matches) return;

        var copies = Array.prototype.slice.call(shell.querySelectorAll('[data-story-copy]'));
        var backgrounds = Array.prototype.slice.call(shell.querySelectorAll('[data-story-bg]'));
        var overlays = Array.prototype.slice.call(shell.querySelectorAll('[data-story-overlay]'));
        var dots = Array.prototype.slice.call(shell.querySelectorAll('[data-story-dot]'));
        var labels = Array.prototype.slice.call(shell.querySelectorAll('[data-story-label]'));
        var growthCounters = Array.prototype.slice.call(shell.querySelectorAll('[data-growth-count]'));
        var heroWords = Array.prototype.slice.call(shell.querySelectorAll('[data-hero-word]'));
        var total = copies.length;

        function formatGrowth(value, format) {
          if (format === 'millions') {
            return (value / 1000000).toFixed(value > 1900000 ? 0 : 1).replace('.0', '') + 'M+';
          }

          if (format === 'thousands') {
            return Math.round(value / 1000) + 'K+';
          }

          if (format === 'comma') {
            return Math.round(value).toLocaleString('en-US') + '+';
          }

          return Math.round(value) + '+';
        }

        function apply(nextIndex, progress) {
          var localProgress = clamp((progress * total) - nextIndex, 0, 1);
          var markerFrom = beatMarkers[nextIndex] || beatMarkers[0];
          var marker = {
            x: markerFrom.x,
            y: markerFrom.y
          };

          if (nextIndex === 0) {
            var markerTo = beatMarkers[1] || markerFrom;
            var heroMarkerAmount = smoothstep(clamp((localProgress - 0.78) / 0.22, 0, 1));
            marker = {
              x: lerp(markerFrom.x, markerTo.x, heroMarkerAmount),
              y: lerp(markerFrom.y, markerTo.y, heroMarkerAmount)
            };
          }

          var heroProgress = nextIndex === 0 ? localProgress : nextIndex > 0 ? 1 : 0;
          var growthProgress = nextIndex < 2 ? 0 : nextIndex > 2 ? 1 : localProgress;
          var easedGrowth = 1 - Math.pow(1 - growthProgress, 3);

          shell.dataset.active = String(nextIndex);
          shell.style.setProperty('--story-progress', String(progress));
          shell.style.setProperty('--hero-word-progress', String(heroProgress));
          shell.style.setProperty('--growth-progress', String(growthProgress));
          shell.style.setProperty('--marker-x', marker.x + '%');
          shell.style.setProperty('--marker-y', marker.y + '%');
          shell.style.setProperty('--ball-size', markerFrom.ballSize || 'clamp(4.8rem, 5.8vw, 5.8rem)');
          var slideTwoFly = nextIndex === 1 ? smoothstep(clamp((localProgress - 0.22) / 0.58, 0, 1)) : 0;
          var ballOpacity = nextIndex === 0 ? 1 : nextIndex === 1 ? (1 - slideTwoFly) : 0;
          shell.style.setProperty('--ball-opacity', String(ballOpacity));
          shell.style.setProperty('--ball-fly-scale', String(1 - (slideTwoFly * 0.78)));
          shell.style.setProperty('--ball-fly-rotate', (slideTwoFly * 720) + 'deg');
          var activeMask = beatMarkers[nextIndex] || marker;
          var activeMaskSize = activeMask.maskSize || 0;
          shell.style.setProperty('--mask-x', (activeMask.maskX || activeMask.x) + '%');
          shell.style.setProperty('--mask-y', (activeMask.maskY || activeMask.y) + '%');
          shell.style.setProperty('--mask-size', activeMaskSize + 'vw');
          shell.style.setProperty('--mask-opacity', activeMaskSize > 0 ? '0.98' : '0');
          shell.style.setProperty('--ball-rotation', (progress * 520) + 'deg');

          heroWords.forEach(function (word, wordIndex) {
            var thresholds = [0.18, 0.46, 0.74];
            var threshold = thresholds[wordIndex] || 0.74;
            word.setAttribute('data-visible', heroProgress >= threshold ? 'true' : 'false');
          });

          growthCounters.forEach(function (counter) {
            var target = Number(counter.getAttribute('data-growth-target')) || 0;
            var format = counter.getAttribute('data-growth-format') || 'plain';
            counter.textContent = formatGrowth(target * easedGrowth, format);
          });

          copies.forEach(function (copy, index) {
            copy.setAttribute('data-state', index === nextIndex ? 'active' : 'inactive');
          });

          backgrounds.forEach(function (background, index) {
            var active = index === nextIndex;
            background.setAttribute('data-state', active ? 'active' : 'inactive');

            if (background.tagName === 'VIDEO') {
              if (active) {
                var playPromise = background.play();
                if (playPromise && typeof playPromise.catch === 'function') {
                  playPromise.catch(function () {});
                }
              } else {
                background.pause();
              }
            }
          });

          overlays.forEach(function (overlay, index) {
            var active = index === nextIndex;
            overlay.hidden = !active;
            overlay.setAttribute('data-state', active ? 'active' : 'inactive');
          });

          dots.forEach(function (dot, index) {
            dot.setAttribute('data-state', index === nextIndex ? 'active' : 'inactive');
          });

          labels.forEach(function (label, index) {
            label.setAttribute('data-state', index === nextIndex ? 'active' : 'inactive');
          });
        }

        function update() {
          var rect = shell.getBoundingClientRect();
          var scrollable = shell.offsetHeight - window.innerHeight;
          var progress = scrollable > 0 ? clamp(-rect.top / scrollable, 0, 1) : 0;
          var index = Math.min(total - 1, Math.floor(progress * total));
          apply(index, progress);
        }

        var ticking = false;
        function requestUpdate() {
          if (ticking) return;
          ticking = true;
          window.requestAnimationFrame(function () {
            update();
            ticking = false;
          });
        }

        window.addEventListener('scroll', requestUpdate, { passive: true });
        window.addEventListener('resize', requestUpdate);
        update();
      }

      initMobileStoryProgress();
      initDesktopStory();
    })();
  </script>
</BaseLayout>
